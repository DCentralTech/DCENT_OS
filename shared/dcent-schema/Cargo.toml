[package]
name = "dcent-schema"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0"
description = "Shared DCENT control-plane schemas for cross-firmware APIs"

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
